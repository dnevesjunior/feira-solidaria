# Restore testado — 2026-08-25

Critério do Epic 0.8: "backup não testado não é backup".

| | |
|---|---|
| Origem | `pg_dump -Fc` do banco de produção (`feira-db`, VPS 67.205.134.61), via SSH |
| Tamanho | 69 KB (banco recém-migrado, sem dados de uso) |
| Destino | PostgreSQL 16 descartável em Docker, máquina do autor (`bin/restore-check`) |
| Duração do restore | < 1 s |
| Resultado | `users: 0`, `events: 0`, `page_views: 0`, `trigger events_append_only: 1` |

Conclusão: o dump restaura em ambiente limpo e o trigger que impõe o
append-only do log de eventos sobrevive ao restore (é parte do `structure.sql`
e do dump).

Observações:

- Na primeira execução o script falhou porque a imagem oficial do Postgres sobe
  um servidor temporário na inicialização e o reinicia; `pg_isready` respondia
  cedo demais. O script passou a exigir duas consultas seguidas com sucesso.
- O backup automático (cron às 03:00 via `/opt/feira/bin/backup`) depende de
  `bin/host-setup` ter sido executado no host. Repetir este teste com um dump
  gerado pelo cron assim que houver um.
- Próximo restore-check: ao final do Epic 1, com imagens no volume de storage.
